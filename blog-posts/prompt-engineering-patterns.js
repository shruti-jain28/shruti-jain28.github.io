window.Portfolio = window.Portfolio || {};
window.Portfolio.POST_CONTENT = {
  id: 'prompt-engineering-patterns',
  content: `
    <p>Every few weeks a new prompting technique gets announced with benchmarks that make it look like the technique has solved reasoning entirely. Chain-of-thought. Tree-of-thought. Self-consistency. Constitutional AI. Reflection. Each one has a paper, usually from a well-regarded lab, showing impressive results on standardised benchmarks. Then you integrate it into something real and discover it works great on the benchmark distribution and fails on your users' actual inputs in ways the benchmark couldn't anticipate.</p>
    <p>This is not a critique of the research — benchmark results are valid in their context. It's a note about the gap between "demonstrated on a controlled evaluation set" and "deployed reliably in an enterprise product used by thousands of people with varied and unpredictable inputs." Here's what we've found to be reliably useful after deploying several LLM-powered features.</p>

    <h2>The Schema Pattern</h2>
    <p>Ask the model to fill in a schema you control, not to generate free-form output. Provide the schema as a JSON template with type annotations and descriptions for each field. Include one or two complete examples of ideal completions. This is the highest-reliability pattern we've found for structured output generation. It constrains the output space without constraining the model's reasoning, which seems to be the right balance: the model can still think through the problem, but the format it delivers the answer in is predictable and validatable.</p>
    <p>The schema pattern also makes your validation layer trivial to write. You're checking whether the output conforms to a known structure, not trying to parse free-form text that might express the same information in any of a hundred different ways. JSON Schema validation or a simple type-checking function catches malformed output before it ever reaches your application logic. Combined with a retry loop for validation failures, this pattern gives you robust structured generation that degrades gracefully rather than catastrophically.</p>

    <h2>The Persona Boundary Pattern</h2>
    <p>When your model needs to behave differently depending on context — a catalog item generator versus a troubleshooting assistant versus a documentation summariser — resist the urge to handle this with conditional logic buried in the prompt. Instead, define a clear persona with explicit scope limitations at the top of every system message. "You are a ServiceNow catalog item generator. You produce catalog item definitions in JSON format. You do not answer questions unrelated to catalog items. If the user asks something outside this scope, respond with: 'I can only help with catalog item generation.'"</p>
    <p>Short. Unambiguous. Models follow hard scope boundaries much more reliably than soft ones. "Try to focus on X" is a soft boundary. "You only do X" is a hard boundary. The difference in behaviour is meaningful, especially for adversarial inputs where users are deliberately trying to get the model to do something outside its intended scope. Hard persona boundaries also make your feature's capabilities legible to users — they understand quickly what the system is for and what it isn't for.</p>

    <h2>The Critic Pattern</h2>
    <p>Generate, then criticise, then revise. Two-pass generation consistently outperforms single-pass for complex outputs where quality is more important than latency. First pass: generate the output with a standard prompt. Second pass: prompt the model to critique the first output against a specific rubric — not a vague "is this good?" but a structured checklist of properties the output should have. Third pass: revise based on the critique. The final output incorporates both the generation and the self-correction.</p>
    <p>We implemented the critic pattern as a background process for use cases where the user isn't waiting in real time — document generation, bulk processing, scheduled tasks. For interactive use cases, we found that a simpler version — one generation pass with a brief self-check prompt appended — captured most of the benefit with significantly less latency overhead.</p>

    <h2>The Few-Shot Anchor</h2>
    <p>Few-shot examples do more than show the model what format to use. They anchor the model's behavior toward a specific level of detail, tone, and specificity. A model given three examples of terse, technical catalog items will generate terse, technical catalog items. A model given three examples of verbose, user-friendly descriptions will generate verbose, user-friendly descriptions — even if the prompt says nothing explicit about length or tone. The examples are doing implicit instruction that text often can't do as reliably.</p>
    <p>The corollary: your examples are load-bearing. Include an example with a mistake corrected in revision — the model learns to self-correct. Include an example that handles an edge case gracefully — the model learns to handle similar edge cases. The investment in curating good examples pays dividends on every request that uses them.</p>

    <h2>What Doesn't Hold Up</h2>
    <p>Zero-shot chain-of-thought — "let's think step by step" — is inconsistent on structured tasks. It improves reasoning on open-ended problems but tends to overgenerate on tasks where the answer is constrained. The model reasons its way into wrong answers it shouldn't have considered at all. Use it for reasoning-heavy tasks; skip it for tasks where the output space is well-defined by your schema or examples.</p>
    <p>Mega-prompts — single system messages that try to specify every possible behaviour the model should exhibit — degrade over time as you add more edge cases. The model's attention is finite; a system message that tries to cover everything tends to reliably cover nothing. Keep system messages focused on the core persona and critical constraints. Handle edge cases in examples rather than in rule lists.</p>
    <p>Finally: temperature zero isn't always better for structured tasks. Fully deterministic output is sometimes exactly what you want, but for creative generation tasks, a small amount of randomness produces better variety without sacrificing quality. Tune temperature based on whether your task benefits from variation or requires consistency, not based on a blanket assumption that lower is more reliable.</p>
  `
};
