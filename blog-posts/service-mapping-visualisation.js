window.Portfolio = window.Portfolio || {};
window.Portfolio.POST_CONTENT = {
  id: 'service-mapping-visualisation',
  content: `
    <p>A service map that displays five hundred nodes is technically complete and practically useless. You can zoom in and out forever, you can filter by type, you can change the layout algorithm, and it still looks like someone dropped a plate of spaghetti on a whiteboard. This is the central tension in infrastructure visualisation: the system you're mapping is genuinely complex, and any simplification you apply risks hiding something that an operator actually needs to see.</p>
    <p>Building workspace modules for Service Mapping at ServiceNow taught me more about information architecture than any book on the subject, largely because the feedback from real operators was immediate and unambiguous. "I can't find anything" is a design problem that no amount of feature documentation will solve.</p>

    <h2>The ML Confidence Problem</h2>
    <p>Our service mapping module had to display not just confirmed infrastructure relationships but candidate relationships — connections suggested by a machine learning model with varying confidence scores. This created a visualisation challenge that doesn't come up in most graph rendering tutorials: how do you show uncertain information alongside certain information without making the uncertain information look certain, and without making the graph so visually cluttered that it becomes unreadable?</p>
    <p>We settled on a visual language that treated confidence as a first-class visual property. Confirmed edges were solid lines with full opacity. High-confidence candidates were dashed lines. Low-confidence candidates were faintly dotted, almost invisible at the default zoom level. Line weight varied with confidence — high-confidence connections were slightly bolder than low. The result was a graph where a quick glance gave you the confirmed topology, and a deliberate examination revealed the ML-suggested possibilities. Operators could choose how much attention to give to the uncertain data based on their context and goals.</p>

    <h2>Progressive Disclosure</h2>
    <p>The instinct with complex data is to show everything and let users filter down. This is almost always wrong. The cognitive overhead of processing a maximally-complex initial view means users spend their first thirty seconds just orienting, not working. By the time they've found a starting point, they've already formed a frustrated relationship with the interface.</p>
    <p>We started with a collapsed view that showed only the top-level service clusters — groups of components that the ML model had identified as likely belonging to the same logical service. Individual components were hidden inside their clusters until the user chose to expand them. The immediate result was that operators could orient themselves within seconds: here are your five services, roughly where they sit in the topology, roughly how they connect. Expanding one cluster to investigate a problem is a deliberate act that gives you detail when you want it, without imposing detail when you don't.</p>
    <p>Time-to-first-useful-action dropped by about forty percent in usability testing. More meaningfully, the qualitative feedback shifted from "overwhelming" to "clear." These two outcomes are related: progressive disclosure doesn't reduce the information available to the user, it changes when the user encounters it.</p>

    <h2>Performance at Scale</h2>
    <p>Rendering five hundred SVG nodes with interactive hover states, pan, zoom, and edge routing in a browser is a performance challenge. SVG is excellent for small-to-medium graphs where the DOM overhead is manageable. For large graphs, the DOM becomes a bottleneck: each node is a DOM element, each edge is a DOM element, and event handling across thousands of DOM elements creates a frame budget problem that no amount of CSS optimisation will solve.</p>
    <p>We moved to a canvas-based renderer for graphs above a configurable threshold — around two hundred nodes by default. The canvas renders the full graph as pixels rather than DOM nodes, which dramatically reduces the rendering cost. Interactivity requires more careful implementation on canvas — you can't just add an event listener to a node because canvas nodes aren't DOM elements — but the hit-testing logic is straightforward and the performance improvement is dramatic. We went from an unusable eight frames per second to a consistent sixty on the same hardware.</p>
    <p>The transition between SVG and canvas rendering is invisible to the user. We kept SVG for the selected node and its immediate connections — the part of the graph the user is actively interacting with — and rendered everything else on canvas. This gave us the interactive fidelity of SVG where it mattered and the performance of canvas everywhere else.</p>

    <h2>The Operator's Mental Model</h2>
    <p>The most important design insight from this project had nothing to do with rendering or algorithms. It came from watching operators actually use the tool during usability sessions. They didn't think about infrastructure as a graph. They thought about it as a story: something is broken, I need to find out what caused it and what it affects. The graph was a means to an end, not an end in itself.</p>
    <p>Every visualisation decision we made after that insight was filtered through the question: does this help an operator answer the question they're actually asking? Confidence indicators help because they tell you which relationships to trust when tracing a failure. Progressive disclosure helps because it lets you start with the service level and drill down toward the component that's misbehaving. Performance helps because a slow tool interrupts the narrative of investigation at the worst possible moments. Visualisation design, it turns out, is storytelling design.</p>
  `
};
